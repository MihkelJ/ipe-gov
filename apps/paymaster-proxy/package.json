{
  "name": "@ipe-gov/paymaster-proxy",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@ipe-gov/sdk": "workspace:*",
    "hono": "^4.6.14",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251022.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.0.0"
  }
}
